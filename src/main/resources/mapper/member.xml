<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.home.mapper.MemberMapper">

	<select id="login" parameterType="member" resultType="member">
		select * from member
		where userid = #{id} and userpwd = #{pw}
	</select>
<!-- 
	<select id="getMemberById" parameterType="String" resultType="member">
		SELECT * FROM member WHERE member_id = #{member_id}
	</select>

	<update id="updateMember" parameterType="member">
		update member set member_name=#{member_name}, password=#{password}, email=#{email}, mobile=#{mobile}
		where member_id = #{member_id}
	</update>
	 -->
</mapper>
